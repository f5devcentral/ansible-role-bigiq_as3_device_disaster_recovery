---
validate_certs: false
timeout: 120

dir_as3: ~/tmp # Working directory to store migration files on your local machine

current_as3_target: 10.1.1.7 # device which needs the RMA
new_as3_target: 10.1.1.8 # new active device
